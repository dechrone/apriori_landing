import { WaitlistPage } from "@/components/WaitlistPage";

export const metadata = {
  title: "Join the Apriori waitlist",
  description:
    "Apriori is the pre-analytics layer for product teams. Test any flow with 50+ synthetic users in hours — not weeks, not post-launch. Join the waitlist.",
};

export default function JoinWaitlist() {
  return <WaitlistPage />;
}
